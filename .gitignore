node_modules/
coverage/
dist/
*.log
.env
.env.*
.DS_Store

# local agent environment (Node 20 pin)
.claude/settings.local.json

# P107 · the android project is committed — it is `cap add`'s output, not a
# build artefact, and regenerating it would lose anything hand-edited later.
# Its build output is not: gradle's caches, and the SDK path, which is one
# machine's absolute path and no other's.
android/app/build/
android/.gradle/
android/local.properties

# the harness parks isolated agent checkouts here — a second copy of this
# repo inside the first one, and not something to commit.
.claude/worktrees/
