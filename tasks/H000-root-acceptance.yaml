id: H000
card: H000
title: Root acceptance goes green
lane: factory
kind: verification
deps: [H004b, H006b]
scope: []
acceptance: tests/acceptance/H000.root.test.ts
done_when: npx vitest run tests/acceptance/H000.root.test.ts
goal: |
  VERIFY ONLY. Write no code.

  Run the root acceptance first (AGENTS.md law 2). If it is green, you are
  done: change nothing, report "already satisfied", open no code diff. That
  is this card working exactly as designed — the work was carried by
  H001..H008 and the correct output is the observation.

  If it is RED, do not fix it. Report precisely which assertion failed and
  which card owns the code that failed. That is a finding, and it is worth
  more than a patch.
notes: |
  scope is empty on purpose. task-lint will fail you for touching anything.
